{
  "default": {
    "user": {"ENV": "PGUSER"},
    "password": {"ENV": "PGPASSWORD"},
    "host": {"ENV": "PGHOST"},
    "port": {"ENV": "PGPORT"},
    "database": {"ENV": "PGDATABASE"},
    "schema": "eligibility_engine",
    "migrationsTable": "eligibility_engine_pgmigrations",
    "migrationsDir": "migrations",
    "migrationFileLanguage": "cjs",
    "createSchema": true,
    "createMigrationsSchema": true
  }
}
